apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mongodb
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
secretGenerator:
  - name: mongodb-secret
    literals:
      - username=admin
      - password=ChangeMe_Str0ng!
